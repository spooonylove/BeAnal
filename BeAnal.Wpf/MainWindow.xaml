<Window x:Class="BeAnal.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeAnal.Wpf"
        mc:Ignorable="d"
        Title="BeAnal" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">
    <Grid>
        <Canvas x:Name="SpectrumCanvas">
            <Canvas.Background>
                <SolidColorBrush x:Name="CanvasBackgroundBrush" Color="Black" />
            </Canvas.Background>
            <Canvas.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Settings..." Click="Settings_Click"/>
                    <MenuItem Header="Exit" Click="Exit_Click"/>
                </ContextMenu>
            </Canvas.ContextMenu>
        </Canvas>

        <ResizeGrip HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom" 
                    Cursor="SizeNWSE" 
                    Foreground="Gray"/>
    </Grid>
</Window>
